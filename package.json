{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "github-pages-deploy-action": "^3.5.1",
    "turbo": "^1.7.4"
  }
}